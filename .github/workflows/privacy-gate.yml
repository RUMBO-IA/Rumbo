name: Public privacy gate

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      base_sha:
        description: 'Exact base commit for the privacy range'
        required: false
        type: string
      commit_sha:
        description: 'Exact candidate commit to verify'
        required: false
        type: string

permissions:
  contents: read

jobs:
  privacy:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v7
        with:
          persist-credentials: false
          ref: ${{ inputs.commit_sha || github.event.pull_request.head.sha || github.sha }}
          fetch-depth: 0
      - uses: actions/setup-python@v7
        with:
          python-version: '3.12'
      - name: Verify public privacy invariants
        env:
          RUMBO_PRIVACY_DENY_HASHES: ${{ secrets.RUMBO_PRIVACY_DENY_HASHES }}
          RUMBO_PRIVACY_COMMIT_SHA: ${{ inputs.commit_sha || github.event.pull_request.head.sha || github.sha }}
          RUMBO_PRIVACY_BASE_SHA: ${{ inputs.base_sha || github.event.pull_request.base.sha || github.event.before }}
        run: python scripts/verify_public_privacy.py
      - name: Verify security headers configuration
        run: python scripts/verify_security_headers.py
